apiVersion: batch/v1
kind: Job
metadata:
  name: "{{ .Values.jobName }}-2"
spec:
  template:
    spec:
      containers:
      - name: "{{ .Values.jobName }}"
        image: perl
        command: ["perl",  "-Mbignum=bpi", "-wle", "sleep(500);"]
      restartPolicy: Never
  backoffLimit: 4
